export const newnewnew = makeActionCreator(({ body, ...res }) => ({
    type: new_new_new,
    method: 'new',
    url: 'new',
    body,
    ...res,
}));

export const oldoldoldnew = makeActionCreator(({ body, ...res }) => ({
    type: OLD_OLD_OLD,
    method: 'old',
    url: 'old',
    body,
    ...res,
}));